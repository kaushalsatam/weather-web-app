<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ClearVue: Weather Insights</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="styles/style.css" />
  </head>
  <body>
    <div class="container">
      <form action="/get-weather" method="post">
        <input
          type="text"
          name="location"
          id="location"
          placeholder="Enter your location"
        />
        <input type="submit" value="Search" />
      </form>
      <% if(locals.content) { %>
      <div class="weather-info">
        <h1><%= locals.content.location.name%></h1>
        <img
          src="<%= locals.content.current.condition.icon%>"
          alt="Current Condition"
        />
        <%= locals.content.current.condition.text%>
        <%= locals.content.current.temp_c%>°C
        <h2>
          <%= locals.content.location.region%>, <%=locals.content.location.country%>
        </h2>
      </div>
      <% } else { %>
      <span><%= locals.error %></span>
      <% } %>
    </div>
  </body>
</html>
